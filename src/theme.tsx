import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#A6CF98",
      dark: "#557C55",
      light: "#F2FFE9",
    },
    secondary: {
      main: "#DB6B97",
    },
  },
});
